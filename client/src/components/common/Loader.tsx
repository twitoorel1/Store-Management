import React from 'react';

const Loader = () => {
	return (
		<section className="absolute top-0 right-0 flex place-items-center justify-center w-full h-full">
			<span className="loader"></span>
		</section>
	);
};

export default Loader;
